name: Testing workflow
on:
  workflow_dispatch
env:
  cloud:google-cloud

jobs:
  greeting_job:
  runs-on: ubuntu-latest
   env:
   greeting:Hello
   steps:
  - name:"says hello guna it's tuesday"
    run:echo"$Greeting $First_Name is using $cloud"
    env:
     First_Name: Ragul
